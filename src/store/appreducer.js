const initialState = {
    items: [
        
    ],
    events:  [

    ],
    characters: [
        
    ]
}
function appReducer(state = initialState, action){
    switch (action.type) { 
            default: 
            return state;
    } 
}
export default appReducer;